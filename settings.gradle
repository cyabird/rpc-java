include 'rpc-demo'
include 'rpc-registry'
include 'rpc-core'
include 'rpc-context'

rootProject.name = 'rpc-project'

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}

